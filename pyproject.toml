[project]
name = "fuiagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "openai>=2.53.0",
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
    "textual>=8.2.8",
]

[project.scripts]
chaldea = "chaldea.tui:main"

[tool.uv]
package = true

[dependency-groups]
dev = ["pytest>=8"]
